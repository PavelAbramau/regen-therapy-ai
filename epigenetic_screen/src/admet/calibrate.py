from __future__ import annotations

import pandas as pd


def calibrate_admet_scores(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder calibration hook; pass-through in v1."""
    return df.copy()

